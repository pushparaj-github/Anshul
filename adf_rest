]# ------------------------ VARIABLES ------------------------
$subscriptionId = "<your-subscription-id>"
$resourceGroup = "<your-resource-group-name>"
$dataFactoryName = "<your-adf-name>"
$apiVersion = "2018-06-01"

# Get Bearer Token using Azure CLI (Service Principal)
$token = (az account get-access-token --query accessToken -o tsv)
$headers = @{ Authorization = "Bearer $token" }

$baseUri = "https://management.azure.com/subscriptions/$subscriptionId/resourceGroups/$resourceGroup/providers/Microsoft.DataFactory/factories/$dataFactoryName"

# ------------------------ TOPOLOGICAL SORT FUNCTION ------------------------
function TopoSort($depMap) {
    $inDegree = @{}
    $graph = @{}

    foreach ($item in $depMap.Keys) {
        $graph[$item] = $depMap[$item]
        if (-not $inDegree.ContainsKey($item)) { $inDegree[$item] = 0 }
        foreach ($dep in $depMap[$item]) {
            if (-not $inDegree.ContainsKey($dep)) { $inDegree[$dep] = 0 }
            $inDegree[$dep]++
        }
    }

    $queue = [System.Collections.Generic.Queue[string]]::new()
    foreach ($node in $inDegree.Keys) {
        if ($inDegree[$node] -eq 0) { $queue.Enqueue($node) }
    }

    $sorted = @()
    while ($queue.Count -gt 0) {
        $node = $queue.Dequeue()
        $sorted += $node
        foreach ($neighbor in $graph[$node]) {
            $inDegree[$neighbor]--
            if ($inDegree[$neighbor] -eq 0) {
                $queue.Enqueue($neighbor)
            }
        }
    }

    return $sorted
}

# ------------------------ STEP 1: DISABLE AND DELETE TRIGGERS ------------------------
Write-Output "`n--- Disabling and Deleting Triggers ---"
$triggers = (Invoke-RestMethod -Uri "$baseUri/triggers?api-version=$apiVersion" -Headers $headers).value
foreach ($trigger in $triggers) {
    $name = $trigger.name
    Write-Output "Stopping trigger: $name"
    Invoke-RestMethod -Method Post -Uri "$baseUri/triggers/$name/stop?api-version=$apiVersion" -Headers $headers -ErrorAction SilentlyContinue
    Start-Sleep -Seconds 2
    Write-Output "Deleting trigger: $name"
    Invoke-RestMethod -Method Delete -Uri "$baseUri/triggers/$name?api-version=$apiVersion" -Headers $headers
}

# ------------------------ STEP 2: DELETE PIPELINES (DEPENDENCY ORDER) ------------------------
Write-Output "`n--- Analyzing and Deleting Pipelines ---"
$pipelines = (Invoke-RestMethod -Uri "$baseUri/pipelines?api-version=$apiVersion" -Headers $headers).value
$pipelineDeps = @{}

foreach ($pipeline in $pipelines) {
    $name = $pipeline.name
    $details = Invoke-RestMethod -Uri "$baseUri/pipelines/$name?api-version=$apiVersion" -Headers $headers
    $dependencies = @()

    foreach ($activity in $details.properties.activities) {
        if ($activity.type -eq "ExecutePipeline") {
            $dependencies += $activity.typeProperties.pipeline.referenceName
        }
        elseif ($activity.inputs) {
            $dependencies += $activity.inputs | ForEach-Object { $_.referenceName }
        }
        elseif ($activity.outputs) {
            $dependencies += $activity.outputs | ForEach-Object { $_.referenceName }
        }
    }
    $pipelineDeps[$name] = $dependencies
}

$sortedPipelines = TopoSort $pipelineDeps
foreach ($name in ($sortedPipelines | Reverse)) {
    Write-Output "Deleting pipeline: $name"
    Invoke-RestMethod -Method Delete -Uri "$baseUri/pipelines/$name?api-version=$apiVersion" -Headers $headers
}

# ------------------------ STEP 3: DELETE DATASETS ------------------------
Write-Output "`n--- Analyzing and Deleting Datasets ---"
$datasets = (Invoke-RestMethod -Uri "$baseUri/datasets?api-version=$apiVersion" -Headers $headers).value
$datasetDeps = @{}

foreach ($dataset in $datasets) {
    $name = $dataset.name
    $details = Invoke-RestMethod -Uri "$baseUri/datasets/$name?api-version=$apiVersion" -Headers $headers
    $linked = $details.properties.linkedServiceName.referenceName
    $datasetDeps[$name] = @($linked)
}

$sortedDatasets = TopoSort $datasetDeps
foreach ($name in ($sortedDatasets | Reverse)) {
    Write-Output "Deleting dataset: $name"
    Invoke-RestMethod -Method Delete -Uri "$baseUri/datasets/$name?api-version=$apiVersion" -Headers $headers
}

# ------------------------ STEP 4: DELETE DATA FLOWS (IF ANY) ------------------------
Write-Output "`n--- Deleting Data Flows (if any) ---"
$dataflows = (Invoke-RestMethod -Uri "$baseUri/dataflows?api-version=$apiVersion" -Headers $headers -ErrorAction SilentlyContinue).value
foreach ($df in $dataflows) {
    $name = $df.name
    Write-Output "Deleting dataflow: $name"
    Invoke-RestMethod -Method Delete -Uri "$baseUri/dataflows/$name?api-version=$apiVersion" -Headers $headers
}

# ------------------------ STEP 5: DELETE LINKED SERVICES ------------------------
Write-Output "`n--- Deleting Linked Services ---"
$linkedServices = (Invoke-RestMethod -Uri "$baseUri/linkedservices?api-version=$apiVersion" -Headers $headers).value
foreach ($ls in $linkedServices) {
    $name = $ls.name
    Write-Output "Deleting linked service: $name"
    Invoke-RestMethod -Method Delete -Uri "$baseUri/linkedservices/$name?api-version=$apiVersion" -Headers $headers
}

Write-Output "`n✅ All ADF resources (except IR) deleted successfully."
